<h2>
  <%= @user.last_name %><%= @user.first_name %>さんの会員詳細
</h2>

<table>
  <tr>
    <th>会員ID</th>
    <td><%= @user.id %></td>
  </tr>
  <tr>
    <th>氏名</th>
    <td><%= @user.last_name %> <%= @user.first_name %></td>
  </tr>
  <tr>
    <th>フリガナ</th>
    <td><%= @user.kana_last_name %> <%= @user.kana_first_name %></td>
  </tr>
  <tr>
    <th>メールアドレス</th>
    <td><%= @user.email %></td>
  </tr>
  <tr>
    <th>会員ステータス</th>
    <td>
      <% if @user.is_deleted %>
      退会
      <% else %>
      有効
      <% end %>
    </td>
  </tr>
</table>

<div>
  <li><%= link_to "編集する", edit_admin_user_path(@user.id) %></li>
  <li><%= link_to "注文履歴一覧を見る", admin_root_path %></li>
</div>